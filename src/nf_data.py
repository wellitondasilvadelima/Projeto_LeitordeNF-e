from dataclasses import dataclass

# creates a dataclass structure to store the values
@dataclass
class data:
      number          : str
      cnpj            : str
      company         : str
      customer_name   : str
      customer_adress : str
      product         : str
      quant_product   : str
      gross_weight    : str
      total           : str
      total_taxable   : str
